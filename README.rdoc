== README
use "foreman start" to run server


generate projects scaffold with :name, :description, :image, :url
	guest access /projects through projects#index
	admin access :new :show :create :edit :update :destroy


install paperclip gem to attatch image to projects model